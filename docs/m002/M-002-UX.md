# M-002 — Contrat UX

> **CONTRAT FINAL 2026-09-24** — La source de vérité actuelle est `docs/m002/M-002-FINAL-CONTRACT.md`. Toute section historique de ce document qui décrit `Gamme 1..5 + usageType PAI/PAE`, un nom de référence calculé, une catégorie obligatoire ou « Mon référentiel » est obsolète lorsqu'elle contredit ce contrat final.

**Statut : RECADRAGE QA VALIDÉ — 2026-09-24**  
**Référence :** `docs/m002/M-002-RECARDAGE-QA.md`

## 1. Page Workspace Produits

Deux vues restent disponibles : `Référentiel global` et `Mon référentiel`.

Recherche :

```text
placeholder = Rechercher un produit…
```

Aucun vocabulaire interne `alias` n'est affiché.

## 2. Liste groupée

Un Produit apparaît une seule fois comme groupe avec sa catégorie.

Sous le groupe : références avec Conservation, État/transformation, unité, rendement et Actions. La Gamme n'est pas affichée dans le frontend actif.

Les actions `+ / −` sont liées à la variante. `Voir`/détail Produit reste au niveau du groupe.

Un Produit sans variante opérationnelle peut apparaître dans le Référentiel global avec `Aucune déclinaison exploitable` et une action d'enrichissement lorsque les droits le permettent.

La pagination principale est par Produit afin de ne jamais couper un groupe entre deux pages.

## 3. Recherche précise

`carotte`, `carotte râpée`, `bœuf paleron`, `agneau gigot tranché` doivent retrouver le Produit puis les variantes pertinentes.

Une suggestion prédictive doit rendre immédiatement lisibles le Produit et la Déclinaison. Une recherche précise ouvre ou réduit le groupe sur le résultat pertinent.

## 4. Formulaire Déclinaison

Axes frontend actifs : Variété, Présentation, Type commercial, Calibre/format, Couleur, Désignation de qualité, Pièce/découpe, État/transformation, unité et rendement.

`foodRange` reste disponible côté backend uniquement ; aucun contrôle Gamme n'est exposé dans les formulaires.

## 5. Gouvernance globale

La surface globale continue d'être protégée par les permissions Application Global Produit.

Pour un gouverneur qui appartient aussi à l'équipe Platform, la navigation Platform doit exposer `Référentiel Produits` grâce au futur point d'extension Core.

Le statut Super Admin ne remplace jamais `product_reference_governor`.

## 6. Administration

Le gouverneur peut corriger Produit, catégorie, synonymes métier, Variétés, Caractéristiques dont `CUT`, variantes, lifecycle et contributions.

## 7. Imports

Le parcours d'import reste inspecter → mapper → prévisualiser → arbitrer → confirmer. Le frontend ne propose aucun mapping Gamme ; la compatibilité `foodRange` reste backend uniquement.

## 8. Frontière M-003

Aucune donnée fournisseur, conditionnement commercial ou prix n'apparaît dans cette surface M-002.
